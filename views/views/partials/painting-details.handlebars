<section class="card">
  <a href="/article/{{id}}">
    <header>{{title}}</header>

    <div class="card-body">
      {{!-- <img src="/images/{{filename}}" alt="{{description}}"> --}}
      <p>By: {{user_id}}</p>
    </div>
  </a>
</section>
